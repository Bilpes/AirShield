# PurposeGraph service configuration (development only).
# Production MUST supply the signing key from a KMS/HSM trust anchor.
PURPOSEGRAPH_SIGNING_KEY=airshield-purposegraph-development-key
PURPOSEGRAPH_ENVIRONMENT=development
PURPOSEGRAPH_SESSION_TTL_SECONDS=300
