apiVersion: v1
kind: Service
metadata: {name: airshield-edge, namespace: airshield}
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: airshield-edge}
  ports:
    - {name: https, port: 8001, targetPort: https}
