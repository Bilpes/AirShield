apiVersion: v1
kind: Service
metadata:
  name: airshield-gateway
  namespace: airshield
spec:
  selector:
    app.kubernetes.io/name: airshield-gateway
  ports:
    - {name: https, port: 8080, targetPort: https}
