apiVersion: v1
kind: Service
metadata:
  name: airshield-control-plane
  namespace: airshield
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: airshield-control-plane}
  ports:
    - {name: https, port: 8080, targetPort: https, protocol: TCP}
